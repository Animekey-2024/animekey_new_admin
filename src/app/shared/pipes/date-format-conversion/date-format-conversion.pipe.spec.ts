import { DateFormatConversionPipe } from './date-format-conversion.pipe';

describe('DateFormatConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
