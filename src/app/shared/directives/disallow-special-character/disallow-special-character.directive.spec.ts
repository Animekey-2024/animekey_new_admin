import { DisallowSpecialCharacterDirective } from './disallow-special-character.directive';

describe('DisallowSpecialCharacterDirective', () => {
  it('should create an instance', () => {
    const directive = new DisallowSpecialCharacterDirective();
    expect(directive).toBeTruthy();
  });
});
